import { makeStyles } from 'tss-react/mui';

export default makeStyles()(() => ({
    radioGroup: {
        display: 'flex',
        justifyContent: 'center',
        marginBottom: '-10px',
    },
    button: {
        marginTop: '20px',
        marginLeft: '18px'
    },
}));